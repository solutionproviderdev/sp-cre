import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const MeetingsScreen = () => {
	return (
		<View>
			<Text>MeetingsScreen...</Text>
		</View>
	);
};

export default MeetingsScreen;

const styles = StyleSheet.create({});
